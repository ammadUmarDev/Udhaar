import 'package:flutter/material.dart';

class InterTextFieldSpacing extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 10.0,
      width: double.infinity,
    );
  }
}
